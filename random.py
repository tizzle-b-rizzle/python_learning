import random #imports the module that allows me to use random number generators etc
for x in range(10): #the number in range tells us how many times the below function will happen
    print(random.randint(1, 100)) #prints a random integer between 1 and 99
